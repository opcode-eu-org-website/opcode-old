<article>
<title>Tunelowanie SSH</title>

<p>Możliwości SSH nie kończą się na udostępnieniu zdalnego shella i możliwości bezpiecznego przesyłu plików, umożliwia on także zdalny dostęp do programów działających w trybie graficznym. Wymaga to jednak włączenia opcji przekazywania protokołu X'ów (w przypadku ssh z linii poleceń opcja -X) oraz działającego na maszynie z której się łączymy serwera X'ów (na maszynie docelowej takiego serwera może nie być w ogóle - musza być tylko stosowne biblioteki oraz program który chcemy odpalić). Po uzyskaniu połączenia shellowego z włączoną opcją przekazywania X'ów programy graficzne odpala się tak jak każdy inny program.</p>

<p>SSH umożliwia także tworzenie tuneli przekierowujących połączenia TCP na zadany port komputera z którego wykonujemy połączenie ssh do określonego adresu IP (wraz z portem) dostępnego z maszyny (serwera ssh) do której się łączymy (może być to np. prywatny adres IP dostępny tylko wewnątrz LANu): <code>ssh -L lokalny_port:mapowany_adres_ip:mapowany_port uzytkownik@serwer_ssh</code>. Możliwe jest też tunelowanie w drugą stronę (<code>ssh -R mapowany_port_serwera:adres_ip:port uzytkownik@serwer_ssh</code>) co umożliwia utworzenie (na serwerze ssh) portu nasłuchujacego który umożliwi połączenie do komputera (z którego zestawiony został tunel) do którego może nie być bezpośredniego dostępu (np. za firewallem); należy tutaj zaznaczyć iż domyślnie nasłuchiwanie na zadanym porcie odbywa się tylko lokalnie aby to zmienić należy przed tym portem określić adres nasłuchiwania (lub * dla wszystkich), aby było to skuteczne musi być włączona w konfiguracji serwera opcja "GatewayPorts". Umożliwia to także proste tworzenie swego rodzaju sieci VPN o bardzo dobrym zabezpieczeniu.</p>

<p>Inną intersującą opcją jest wykozystanie ssh w roli proxy którego jeden koniec mamy lokalnie, a drugi gdzieś daleko. Funkconalność taką zapewnia opcja -D w ssh i wykorzystanie lokalnego końca tunelu jako proxy typu SOCKS. Kolejną z ciekawych opcji jest <code>-w</code> pozwalająca na tworzenie urządzeń tunelowych, które mogą posłużyć do realizacji prawdziwego VPN.</p>
</article>
