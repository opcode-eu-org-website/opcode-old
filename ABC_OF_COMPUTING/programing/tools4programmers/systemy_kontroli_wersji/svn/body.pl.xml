<article>
<title>SVN</title>

<p><wiki pl="Subversion">Subversion</wiki> (czyli svn) jest systemem kontroli wersji z centralnym repozytorium.</p>

<pre>
# pobranie repozytorium
svn checkout

# aktualizacja
svn update

# dodawanie i usuiwaniw plików
svn add|rm

# wysyłanie zmian a serwer
svn commit
</pre>

<p>Podstawowymi narzędziami są komendy <code>svn</code> (do zarządzania kopią roboczą) i <code>svnadmin</code> (do zarządzania repozytorium po stronie serwera. Kilka przykładów użycia:</p>
<ul>
	<li>zmiana adresu URL repozytorium w kopii roboczej - <code>svn switch --relocate <var>STARY</var> <var>NOWY</var></code></li>
	<li>historia zmian pliku - <code>svn log <var>PLIK</var></code></li>
	<li>podgląd starszej wersji pliku svn  <code>cat -r <var>revision</var> <var>PLIK</var></code></li>
	<li>wykonanie zrutu z wybranych rewizji (np. tylko aktualnego) - <code>svnadmin dump <var>katalog</var> -r <var>revision</var></code></li>
	<li>import zrzutu (do pustego spowoduje zaimportowanie UUID) - <code>svnadmin load <var>katalog</var></code></li>
</ul>
<p>URL może wskazywać dostęp np. w oparciu o ssh - <code>svn+ssh://<var>host</var>/<var>katalog/repo</var></code>.</p>

</article>
