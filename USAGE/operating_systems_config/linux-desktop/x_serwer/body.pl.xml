<article>
<title>Ustawienia serwera X'ów - ładniejsze czcionki, wiele monitorów, ...</title>
<p>Jeżeli nasze czcionki, pomimo przyzwoitej rozdzielczości wyświetlanego obrazu, nie wyglądają zbyt ładnie, są zbyt małe, opcje antyaliasingu często wręcz pogarszają sytuację, przyczyną może być zbyt mała rozdzielczość optyczna wyświetlanych obrazów (ta wyrażana w <wiki pl="Ppi">ppi</wiki>, niekiedy niepoprawnie nazywanym niekiedy <wiki pl="Dpi">dpi</wiki>). Rozdzielczość tą możemy sprawdzić poleceniem <code>xdpyinfo | grep resolution</code>, rozdzielczość ta wraz z rozdzielczością całego ekranu przekłada się na jego (wirtualne) wymiary (które możemy sprawdzić <code>xdpyinfo | grep dimensions</code>). Aby temu zaradzić w sekcji Monitor pliku konfiguracyjnego serwera X należy określić właśnie tą wielkość ekranu (w mm): <code>DisplaySize X Y</code> gdzie X i Y to odpowiednie wymiary obliczone z zależności X,Y = pikseleX,Y / rozdzielczosc_ppi * 25.4 i przybliżone w dół do liczby całkowitej. Dobrą wartości ppi wydaje się być 96.</p>

<p>X serwer umożliwia także konfigurację środowiska wielomonitorowego. Wymaga to oczywiście odpowiedniego wsparcia sprzętowego - karta z wieloma wyjściami (której sterownik jest zgodny z xrandr lub xinerama), albo zestaw niekłócących się ze sobą kart. Obecnie metoda konfiguracji typu xinerama (ServerLayout złożony z wielu Screen'ów - każdy na osobnym urządzeniu, w szczególności zwielokrotnieniu Device pojedynczej karty z podaniem różnych wartości Screen oznaczających wyjście danej karty) zaczyna odchodzić do historii na rzecz "półautomatycznej" konfiguracji xrandr. Wielomonitoring poprzez xrandr możemy obsługiwać dynamicznie lub z stałą konfiguracją w xorg.conf, przykład takiej konfiguracji dla karty obsługiwanej przez sterownik radeonhd przedstawiam w <a class="int" href="./xorg.conf">xorg.conf</a>.</p>

<p>Komenda xrandr może posłużyć do zmiany rozdzielczości w locie, przełączania ekranów a także ich wyłączania. X serwer umożliwia także uśpienie monitora (przełączenie w tryb oszczędzania energii), możemy tego dokonać komendą: <code>killall xscreensaver ; sleep 1; xset dpms force off</code>, w odróżnieniu od wyłączenia z użyciem xrandr monitor włączy się automatycznie po ruszeniu myszką lub naciśnięciu klawiatury. Xset umożliwia także wyłączenie automatycznego włączania wygaszacza ekranu i przechodzenia w tryb uśpienia (przydatne gdy nasz odtwarzacz filmów nie potrafi tego zablokować): <code>xset s off; xset -dpms</code>. Niektóre operacje (np. regulacja jasności podświetlenia w laptopie) wymagają bezpośredniego odwołania do podsystemu /sys, np. w przypadku Panasonic CF-18: <code>echo 255 > /sys/class/backlight/panasonic/brightness</code> ustawi maksymalną jasność podświetlenia (minimalną zapis zera).</p>
</article>
