<article>
<title>audio</title>
	<p><strong>wydzielenie ścieżki audio z pliku wideo:</strong><br />
		<code>tcextract -a 0 -i "<samp>IN.avi</samp>" -x mp3 &gt; <samp>OUT.mp3</samp></code><br />
	<strong>docinanie plików audio</strong> (program wywala sie przy generowaniu ostatniego pliku ale to niewielka przeszkoda):<br />
		<code>tcmp3cut -i <samp>1.mp3</samp> -o <samp>1a.mp3</samp> -t 330000,1725999 -d 0</code><br />
	<strong>nagrywanie audio:</strong><br />
		<code>rec -t ogg -d /dev/dsp <samp>OUT.ogg</samp></code> (oczywiscie najpier ustawic odpowiednio pierwsze wejscie w mixerze ...)<br />
	<strong>łączenie plików audio (mp3):</strong><br />
		<code>mp3wrap <samp>all.mp3</samp> <samp>1.mp3</samp> <samp>2.mp3</samp> <samp>3.mp3</samp> <samp>4.mp3</samp></code><br />
	<strong>poprawianie pliku mp3:</strong> (metoda brutalna ale skuteczna ...)<br />
		<code>lame --decode <samp>all_MP3WRAP.mp3</samp> ; lame -b64 -h <samp>all_MP3WRAP.mp3.wav</samp></code>
	</p>

</article>
